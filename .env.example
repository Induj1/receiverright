# Local mode needs no AWS credentials. Start with npm install && npm run dev.
PORT=3001
# Only set these to connect the backend to actual AWS resources.
# AWS_PROFILE=receiverright
# AWS_REGION=ap-south-1
# TABLE_NAME=your-dynamodb-table
# EVIDENCE_BUCKET=your-private-versioned-bucket
# BEDROCK_MODEL_ID=apac.amazon.nova-lite-v1:0
MAX_DAILY_AI_CALLS=50
